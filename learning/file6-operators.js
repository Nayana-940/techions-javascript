//Javascript operators are used to perform different types of mathematical and logical computations.

Examples:
//The Assignment Operator = assigns values
//The Addition Operator + adds values
//The Multiplication Operator * multiplies values
//The Comparison Operator > compares values








//The Assignment Operator (=) assigns a value to a variable:

// Assign the value 5 to x
let x = 5;
// Assign the value 2 to y
let y = 2;
// Assign the value x + y to z
let z = x + y;


//Addition Operator (+): Adds two values.

let Sum = a + b; // sum will be 30

//Multiplication Operator (*): Multiplies two values.

let Product = a * b; // product will be 200

//Comparison Operator (>): Compares two values to check if the left value is greater than the right value.

let isGreater = a > b; // isGreater will be false

// Arithmetic Operators in JavaScript

// Addition (+) - Adds two values
let sum = 10 + 5; // sum will be 15
console.log("Sum:", sum);

// Subtraction (-) - Subtracts one value from another
let difference = 10 - 5; // difference will be 5
console.log("Difference:", difference);

// Multiplication (*) - Multiplies two values
let product = 10 * 5; // product will be 50
console.log("Product:", product);

// Division (/) - Divides one value by another
let quotient = 10 / 5; // quotient will be 2
console.log("Quotient:", quotient);

// Modulo (%) - Returns the remainder of a division
let remainder = 10 % 3; // remainder will be 1
console.log("Remainder:", remainder);

// Increment (++) - Increases the value of a variable by 1
let num = 5;
num++; // num will be 6
console.log("Incremented num:", num);

// Decrement (--) - Decreases the value of a variable by 1
num--; // num will be 5 again
console.log("Decremented num:", num);








// Using assignment operators in JavaScript
let a = 10;
console.log("Initial value of a:", a);

// Addition Assignment
a += 5; // a becomes 15
console.log("After a += 5:", a);

// Subtraction Assignment
a -= 3; // a becomes 12
console.log("After a -= 3:", a);

// Multiplication Assignment
a *= 2; // a becomes 24
console.log("After a *= 2:", a);

// Division Assignment
a /= 4; // a becomes 6
console.log("After a /= 4:", a);

// Modulo Assignment
a %= 4; // a becomes 2
console.log("After a %= 4:", a);





